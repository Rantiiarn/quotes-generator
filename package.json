{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "jalankan": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.0.0"
  },
  "dependencies": {
    "@nuxtjs/supabase": "^0.3.0",
    "bootstrap": "^5.2.3"
  }
}
